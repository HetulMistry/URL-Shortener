# Production API URL (ignored in dev — Vite proxy is used automatically)
VITE_API_URL=http://localhost:3000/api/v1

# Public short-link base URL (backend redirect host)
VITE_APP_URL=http://localhost:3000
